node {
  stage 'Checkout'
  git 'ssh://github.com/nadellavamsee/jenkins.git'
 
  stage 'Docker build'
  docker.build('nginx')
 
  stage 'Docker push'
  docker.withRegistry('https://registry.hub.docker.com', 'credentials-id') {
    docker.image('nginx').push('latest')
  }
}
