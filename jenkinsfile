pipeline {
  environment {
    registry = "docker_hub_account/nadellavamsee"
    registryCredential = 'dockerid'
  }
  agent any
  stages {
    stage('Building image') {
      steps{
        script {
          docker.build registry + ":$BUILD_NUMBER"
        }
      }
    }
  }
}
